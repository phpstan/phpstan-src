name: "Claude Easy Fix on Milestone"

on:
  issues:
    types: [milestoned]

jobs:
  fix:
    if: github.event.milestone.title == 'Easy fixes'
    uses: ./.github/workflows/claude-fix-issue.yml
    with:
      issue-number: ${{ github.event.issue.number }}
    secrets: inherit

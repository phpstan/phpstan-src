diff --git a/zend/base.cpp b/zend/base.cpp
index 8199f52..3ee8ef3 100644
--- a/zend/base.cpp
+++ b/zend/base.cpp
@@ -277,7 +277,9 @@ Php::Value Base::__count(Php::Parameters &params)
     if (countable == nullptr) return 0;
 
     // pass the call to the interface
-    return countable->count();
+    // (explicit cast: on LP64 macOS 'long' matches no Php::Value constructor
+    // unambiguously because int64_t is 'long long' there)
+    return (int64_t)countable->count();
 }
 
 /**
